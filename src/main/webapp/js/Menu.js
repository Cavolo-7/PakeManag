//管理员菜单切换
function toggle(url) {
    document.getElementById("iframe").setAttribute("src",url.title)
}
//用户菜单切换
function toggles(url) {
    document.getElementById("iframe").setAttribute("src",url.title)
}
