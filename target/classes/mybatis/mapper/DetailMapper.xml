<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.auc.mapper.DetailMapper">
    <select id="queryFirst" resultType="com.auc.pojo.Detail">
         SELECT detail_event,SUM(detail_money) AS detail_money FROM t_detail WHERE detail_event !='自助缴费'  GROUP BY detail_event
    </select>


<!--    <select id="querySecond" resultType="com.auc.pojo.Produce">-->
<!--        select p.produce_id,p.produce_name,p.produce_describe,p.produce_money,a.param_name from t_produce p,t_param a-->
<!--        <where>-->
<!--            and p.produce_static=a.param_value-->
<!--        </where>-->
<!--        limit #{page},#{limit}-->
<!--    </select>-->

<!--    <select id="querySecondCount" resultType="Integer">-->
<!--        select count(*) from t_produce p,t_param a-->
<!--        <where>-->
<!--            and p.produce_static=a.param_value-->
<!--        </where>-->
<!--    </select>-->

    <select id="queryThird" resultType="com.auc.pojo.Detail">
        SELECT detail_event,SUM(detail_money) AS detail_money FROM t_detail WHERE detail_event ='自助缴费'  GROUP BY detail_event
    </select>

<!--    <select id="queryFourth" resultType="com.auc.pojo.Detail">-->
<!--        select p.produce_id,p.produce_name,p.produce_describe,p.produce_money,a.param_name from t_produce p,t_param a-->
<!--        <where>-->
<!--            and p.produce_static=a.param_value-->
<!--        </where>-->
<!--        limit #{page},#{limit}-->
<!--    </select>-->



</mapper>